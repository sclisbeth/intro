import { LettersDirective } from './letters.directive';

describe('LettersDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersDirective();
    expect(directive).toBeTruthy();
  });
});
